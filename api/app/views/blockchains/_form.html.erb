<%= form_with(model: blockchain, local: true) do |form| %>
  <% if blockchain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blockchain.errors.count, "error") %> prohibited this blockchain from being saved:</h2>

      <ul>
      <% blockchain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
